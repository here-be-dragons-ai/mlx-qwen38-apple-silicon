--- a/mlx_vlm/models/qwen3_5/language.py
+++ b/mlx_vlm/models/qwen3_5/language.py
@@ -1,3 +1,4 @@
+import os
 from functools import lru_cache, partial
 from typing import Any, List, Optional
 
@@ -1030,6 +1031,15 @@
 
 
 def _fused_quantized_linears(linears, x: mx.array):
+    # QWEN38_FUSED_LINEARS=0 schaltet die Fusion ab (lokaler Patch 0015).
+    # Der Cache unten haelt eine ZWEITE, zusammenkopierte Fassung der
+    # quantisierten Gewichte dauerhaft am Modul fest. Auf Qwen3.8-27B sind das
+    # gemessen 9,0 GiB mit Speculative Decoding und 2,1 GiB ohne — bei einem
+    # Durchsatzgewinn, der in der Messstreuung untergeht (26,1 vs 25,7 tok/s
+    # aus je 5 Laeufen). Auf einer 48-GiB-Maschine ist das der schlechtere
+    # Handel, deshalb der Schalter. Default bleibt das Upstream-Verhalten.
+    if os.environ.get("QWEN38_FUSED_LINEARS") == "0":
+        return None
     if (
         x.ndim != 3
         or len(linears) < 2
